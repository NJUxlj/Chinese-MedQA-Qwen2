# Evaluator 模块简化与集中化改造方案 v2.0

> **目标**：将散落在 `src/evaluation/` 的 evaluator 逻辑大幅简化，删除 `evaluation_runner.py` 与 `evaluator_factory.py`，所有 evaluator 移入 `src/utils/`，config.yaml 中评估器配置全部删除，唯一运行入口收敛到 `src/api/routers/evaluation.py`。

---

## 1. 目标与原则

### 1.1 目标

1. **删除非核心 evaluator**：`code_evaluator.py`、`dpo_quality_evaluator.py`、`math_evaluator.py` **直接删除**，不再保留任何代码（详见补充方案二）。仅保留 `bleu_rouge_evaluator.py` 与 `medqa_llm_evaluator.py`。
2. **删除 runner 与工厂**：`evaluation_runner.py`、`evaluator_factory.py` 整体删除，其职责由 `evaluation.py` Router 直接承担。
3. **集中化配置**：`src/config/config.yaml` 中 `evaluator:` 节（第 104–131 行，共 27 个键值对）全部删除，所有评估参数改为从 API Request（Pydantic Model）传入。
4. **统一目录**：所有 `*_evaluator.py` 移入 `src/utils/`，原 `src/evaluation/` 目录仅保留必要文件或整体收缩。
5. **去除 settings 依赖**：所有 `*_evaluator.py`（含 `base_evaluator.py`、`bleu_rouge_evaluator.py`、`medqa_llm_evaluator.py`）中不得再 `from config.settings import settings`，也不得再写模块级 `run()` / `run_evaluation()` 入口函数。
6. **唯一入口**：`src/api/routers/evaluation.py` 是唯一可以驱动 evaluator 运行的地方；通过线程池直接实例化 evaluator 并调用 `evaluate_one_sample()` / `evaluate_batch_samples()`。

### 1.2 原则

- **最小可用**：evaluator 只保留"接收一条样本 → 返回指标字典"的能力，数据集加载、批量遍历、指标聚合全部上提到 Router 层。
- **显式传参**：`__init__(self, config=None)` 改为 `__init__(self, **kwargs)` 或接收扁平 dict，所有参数从调用方显式传入，不隐式读取全局 settings。
- **保持接口稳定**：`evaluate_one_sample(self, sample: Dict[str, Any]) -> Dict[str, Any]` 签名不变，确保 Router 层聚合逻辑通用。

---

## 2. 文件级操作清单

| 文件（原路径） | 最终命运 | 说明 |
|---|---|---|
| `src/evaluation/base_evaluator.py` | **移动到** `src/utils/base_evaluator.py` | 去除 `settings` 依赖，精简 DataLoader 相关逻辑 |
| `src/evaluation/bleu_rouge_evaluator.py` | **移动到** `src/utils/bleu_rouge_evaluator.py` | 删除 `run()` 与 `save_results()`，去除 `settings` 引用 |
| `src/evaluation/medqa_llm_evaluator.py` | **移动到** `src/utils/medqa_llm_evaluator.py` | 删除 `run()`，去除 `settings` 引用 |
| `src/evaluation/code_evaluator.py` | **直接删除** | 不再保留任何代码（详见补充方案二） |
| `src/evaluation/math_evaluator.py` | **直接删除** | 不再保留任何代码（详见补充方案二） |
| `src/evaluation/dpo_quality_evaluator.py` | **直接删除** | 不再保留任何代码（详见补充方案二） |
| `src/evaluation/evaluator_factory.py` | **删除** | 工厂映射逻辑内联到 `evaluation.py` Router |
| `src/evaluation/evaluation_runner.py` | **删除** | 数据加载、聚合逻辑上提到 `evaluation.py`；config 合并逻辑废弃 |
| `src/evaluation/async_eval_queue.py` | **移动到** `src/utils/async_eval_queue.py` | 修改内部 import，移除对 `evaluation_runner` 的依赖 |
| `src/evaluation/inference_utils.py` | **移动到** `src/utils/inference_utils.py` | 内容保持不变，修改 import 路径 |
| `src/evaluation/task_types.py` | **移动到** `src/utils/task_types.py` | 内容保持不变，修改 import 路径 |
| `src/evaluation/__init__.py` | **重写** | 删除 `EvaluatorFactory`、`EvaluationTaskType` 导出，改为空文件或仅保留 docstring |
| `src/evaluation/data/` | **删除** | 测试假数据，不再使用 |
| `src/evaluation/logs/` | **删除** | 日志目录，可由 logger 统一管理 |
| `src/evaluation/run_evaluator/run_math_evaluator.sh` | **删除** | 脚本废弃 |
| `src/api/routers/evaluation.py` | **修改** | 重写核心逻辑：直接实例化 evaluator、在线程池中运行、聚合指标 |
| `src/api/main.py` | **修改** | 更新 `async_eval_queue` 的 import 路径 |
| `src/api/eval_minimal_main.py` | **修改** | 更新 `async_eval_queue` 的 import 路径 |
| `src/config/config.yaml` | **修改** | 删除第 104–131 行 `evaluator:` 节 |

---

## 3. 配置迁移方案

### 3.1 config.yaml 中要删除的键

删除 `evaluator:` 节下的全部 27 个键：

```yaml
# ===== 以下整个 evaluator 节删除 =====
evaluator:
  model_name_or_path: ...
  test_dataset_path: ...
  batch_size: ...
  max_new_tokens: ...
  temperature: ...
  top_p: ...
  metrics: ...
  device: ...
  padding_side: ...
  use_fast: ...
  prompt_key: ...
  ground_true_answer_key: ...
  eval_result_save_path: ...
  dataloader_num_workers: ...
  reference_model_path: ...
  output_dir: ...
  beta: ...
  max_length: ...
  enable_thinking: ...
  query_key: ...
  chosen_key: ...
  rejected_key: ...
  execution_timeout: ...
  max_memory_mb: ...
  sandbox_working_dir: ...
  enable_security_check: ...
```

### 3.2 新的 Pydantic Request 模型设计

在 `src/api/routers/evaluation.py` 中，将 `EvaluationRequest` 扩展为包含所有 evaluator 运行所需的参数。采用 `Field(default=...)` 给出合理默认值，避免调用方必须传全量字段。

```python
from enum import Enum
from typing import Any, Dict, List, Optional
from pydantic import BaseModel, Field


class EvaluationTaskType(str, Enum):
    MEDQA_LLM = "medqa_llm"
    BLEU_ROUGE = "bleu_rouge"
    MATH = "math"
    CODE = "code"
    DPO_QUALITY = "dpo_quality"


class EvaluatorConfig(BaseModel):
    """评估器运行参数，全部扁平化放在 Request 中。"""

    # 通用模型参数
    model_name_or_path: Optional[str] = Field(default=None, description="本地模型路径")
    device: str = Field(default="cpu")
    max_new_tokens: int = Field(default=2048)
    temperature: float = Field(default=0.7)
    top_p: float = Field(default=0.9)

    # vLLM API 参数
    vllm_model_name: Optional[str] = Field(default=None)
    vllm_base_url: Optional[str] = Field(default=None)
    vllm_api_key: Optional[str] = Field(default=None)

    # BLEU / ROUGE / Math 通用字段映射
    prompt_key: str = Field(default="prompt")
    ground_true_answer_key: str = Field(default="answer")

    # Math 专用
    padding_side: str = Field(default="left")
    use_fast: bool = Field(default=True)
    enable_thinking: bool = Field(default=False)
    max_length: int = Field(default=2048)

    # DPO 专用
    beta: float = Field(default=0.1)
    query_key: str = Field(default="query")
    chosen_key: str = Field(default="chosen")
    rejected_key: str = Field(default="rejected")

    # Code 专用
    execution_timeout: int = Field(default=30)
    max_memory_mb: int = Field(default=256)
    sandbox_working_dir: str = Field(default="/tmp/code_eval")
    enable_security_check: bool = Field(default=True)


class EvaluationRequest(BaseModel):
    evaluation_task_id: Optional[str] = Field(default=None)
    task_type: EvaluationTaskType
    dataset_path: str = Field(..., description="JSON 数据集路径")

    # 数据集字段映射
    question_key: str = Field(default="question")
    model_answer_key: str = Field(default="output")
    ground_true_answer_key: str = Field(default="answer")

    # 运行配置
    evaluator_config: EvaluatorConfig = Field(default_factory=EvaluatorConfig)


class EvaluationResponse(BaseModel):
    evaluation_task_id: str
    metrics: Dict[str, float] = Field(default_factory=dict)
    model_name: str = ""
    status: str = "pending"
    process_time: float = 0.0
    additional_info: Optional[Dict[str, Any]] = None
```

**设计说明**：
- `EvaluatorConfig` 将原来散落在 `config.yaml` 的 27 个键全部收拢；Router 在收到请求后，将 `evaluator_config` 转为 `dict` 直接传给各 Evaluator 的 `__init__`。
- `EvaluationTaskType` 不再从 `task_types.py` 导入，而是直接定义在 Router 中（或保留 `task_types.py` 但移动路径），以切断循环依赖。

---

## 4. Evaluator 简化方案

### 4.1 通用改造（适用于所有 evaluator）

1. **删除模块级入口函数**：每个 `*_evaluator.py` 文件底部的 `run()` / `run_evaluation()` / `if __name__ == "__main__":` 全部删除。
2. **删除 `settings` 引用**：移除 `from config.settings import settings`；`__init__(self, config=None)` 中的 `config` 必须显式传入，不再 fallback 到 `settings.evaluator`。
3. **修改 `base_evaluator.py`**：
   - 移除 `from config.settings import settings`
   - `__init__(self, config=None)` 改为 `__init__(self, config: Optional[Dict[str, Any]] = None)`
   - 不再默认加载 `settings.evaluator`；`config` 为 `None` 时，`test_data` 设为空列表，`test_dataloader` 设为 `None`（因为 Router 层不再走 `evaluate()` 遍历数据集，而是逐条调用 `evaluate_one_sample`）
   - 保留 `load_model_and_tokenizer`、`load_test_dataset` 供需要本地模型的子类使用，但不再自动在 `__init__` 中加载数据集

```python
# src/utils/base_evaluator.py（改造后）
import json
from typing import Dict, List, Optional, Any
import torch
from torch.utils.data import DataLoader, Dataset
from utils.logger import setup_logger


class EvaluatorDataset(Dataset):
    def __init__(self, data: List[Dict[str, Any]]):
        self.data = data

    def __len__(self):
        return len(self.data)

    def __getitem__(self, idx):
        return self.data[idx]


class BaseEvaluator:
    """所有评估器的基类。配置必须由调用方显式传入，不再读取全局 settings。"""

    def __init__(self, config: Optional[Dict[str, Any]] = None):
        self.config = config or {}
        self.logger = setup_logger(name=self.__class__.__name__, level="INFO")
        self.model = None
        self.tokenizer = None

    def load_model_and_tokenizer(self):
        from transformers import AutoModelForCausalLM, AutoTokenizer
        model_path = str(self.config.get("model_name_or_path", ""))
        device = str(self.config.get("device", "cpu"))
        padding_side = str(self.config.get("padding_side", "left"))
        use_fast = bool(self.config.get("use_fast", True))

        self.model = AutoModelForCausalLM.from_pretrained(
            model_path,
            torch_dtype=torch.bfloat16,
            device_map=device,
        )
        self.tokenizer = AutoTokenizer.from_pretrained(
            model_path,
            padding_side=padding_side,
            use_fast=use_fast,
        )
        self.tokenizer.pad_token = self.tokenizer.eos_token

    def load_test_dataset(self, dataset_path: str) -> List[Dict[str, Any]]:
        if not dataset_path or dataset_path == "None":
            return []
        if not dataset_path.endswith(".json"):
            raise ValueError(f"目前只支持 .json 格式的数据集: {dataset_path}")
        try:
            with open(dataset_path, "r", encoding="utf-8") as f:
                return json.load(f)
        except json.JSONDecodeError as e:
            raise ValueError(f"JSON 解析错误: {e}")

    def evaluate_one_sample(self, sample: Dict[str, Any]) -> Dict[str, Any]:
        raise NotImplementedError("子类必须实现 evaluate_one_sample 方法")
```

### 4.2 `code_evaluator.py` 【作废，直接删除】

> **说明**：根据补充方案二，`code_evaluator.py` **直接删除**，不再保留任何代码。以下原简化策略内容作废，仅留档参考。

**删除内容**：
- 模块级函数 `run_evaluation()`（第 659–671 行）及 `if __name__ == "__main__":`
- `evaluate()` 方法（第 545–605 行）—— Router 层负责遍历数据集
- `evaluate_batch_samples()` 方法（第 510–543 行）—— Router 层负责循环调用单条
- `evaluate_one_sample_by_api()` / `evaluate_batch_samples_by_api()`（第 607–612 行）—— 与 `evaluate_one_sample` 等价，无需重复
- `evaluate_with_ground_truth()`（第 614–652 行）—— 非核心，业务层不需要
- `cleanup()` 实例方法（第 654–656 行）—— `__del__` + `CodeExecutor.cleanup()` 足够
- 导入中的 `from config.settings import settings`

**保留内容**：
- `CodeSecurityChecker` 类（安全检测仍有价值，保留）
- `CodeExecutor` 类（核心执行沙箱，保留）
- `CodeEvaluator.__init__(self, config)` —— 改为接收 dict，从 dict 取参数
- `CodeEvaluator.evaluate_one_sample(self, sample)` —— 核心评估逻辑保留

**改造后 `__init__` 与 `evaluate_one_sample` 签名**：

```python
class CodeEvaluator(BaseEvaluator):
    def __init__(self, config: Optional[Dict[str, Any]] = None):
        super().__init__(config)
        self.executor = CodeExecutor(
            timeout=int(self.config.get("execution_timeout", 30)),
            max_memory_mb=int(self.config.get("max_memory_mb", 256)),
            working_dir=str(self.config.get("sandbox_working_dir", "/tmp/code_eval")),
        )
        self.supported_languages = ["python", "python3", "javascript", "java", "cpp", "c"]

    def evaluate_one_sample(self, sample: Dict[str, Any]) -> Dict[str, Any]:
        # 与现有逻辑基本一致，仅将 self.config.enable_security_check 改为 self.config.get("enable_security_check", True)
        ...
```

### 4.3 `dpo_quality_evaluator.py` 【作废，直接删除】

> **说明**：根据补充方案二，`dpo_quality_evaluator.py` **直接删除**，不再保留任何代码。以下原简化策略内容作废，仅留档参考。

**删除内容**：
- 模块级函数 `run()`（第 454–459 行）及 `if __name__ == "__main__":`
- `evaluate()` 方法（第 295–314 行）—— Router 层负责遍历
- `evaluate_batch_examples()`（第 282–293 行）—— Router 层负责平均
- `prepare_dpo_batch()`（第 90–131 行）—— 非 API 路径下才有用，但简化后只保留 API 路径或本地路径的核心调用；若保留本地模型路径，可将其内联到 `evaluate_one_sample` 中
- `compute_logps()`（第 133–163 行）—— 仅本地模型路径需要，保留但设为私有 `_compute_logps`
- `compute_rewards()`（第 165–219 行）—— 同上，保留但设为私有 `_compute_rewards`
- `generate_preference_predictions()`（第 316–351 行）—— 非核心
- `generate_comparison_matrix()`（第 353–375 行）—— 非核心
- `visualize_comparison_matrix()`（第 377–406 行）—— 非核心，含 matplotlib
- `save_evaluation_results()`（第 408–426 行）—— Router 层已负责返回结果
- `visualize_reward_distribution()`（第 428–451 行）—— 非核心，含 matplotlib
- `evaluate_batch_samples_by_api()` —— Router 层直接逐条调用即可
- 导入中的 `from config.settings import settings`、`matplotlib.pyplot`、`numpy`（若删除可视化）

**保留内容**：
- `__init__`：根据 `config` dict 初始化模型或 LLMProvider
- `evaluate_one_sample`：本地模型路径（调用 `_compute_rewards` + `metrics.calculate_reward_accuracy`）
- `evaluate_one_sample_by_api`：API 路径（二值偏好近似）
- `_compute_logps`、`_compute_rewards`：若仍需本地 DPO 评估则保留

**注意**：若业务上 DPO 评估目前只用 API 模式，可进一步将本地模型相关代码全部删除，只保留 API 模式；为保险起见，方案中保留双路径但删除所有可视化/矩阵/预测方法。

```python
class DPOQualityEvaluator(BaseEvaluator):
    def __init__(self, config: Optional[Dict[str, Any]] = None):
        super().__init__(config)
        self.beta = float(self.config.get("beta", 0.1))
        self.max_length = int(self.config.get("max_length", 2048))
        self._use_api = bool(self.config.get("_use_vllm_api", False))
        self.device = str(self.config.get("device", "cpu"))
        # ... 模型初始化逻辑保留，但参数从 self.config dict 读取

    def evaluate_one_sample(self, sample: Dict[str, Any]) -> Dict[str, float]:
        if self._use_api:
            return self._evaluate_one_sample_api(sample)
        # 本地路径逻辑保留，但内联准备 batch
        ...

    def _evaluate_one_sample_api(self, sample: Dict[str, Any]) -> Dict[str, float]:
        # 原 evaluate_one_sample_by_api 改名并保留核心逻辑
        ...
```

### 4.4 `math_evaluator.py` 【作废，直接删除】

> **说明**：根据补充方案二，`math_evaluator.py` **直接删除**，不再保留任何代码。以下原简化策略内容作废，仅留档参考。

**删除内容**：
- 模块级函数 `run()`（第 563–567 行）及 `if __name__ == "__main__":`
- `evaluate()` 方法（第 325–357 行）—— Router 层负责遍历
- `get_eval_prompt()`（第 84–95 行）—— 直接 `sample.get(prompt_key, "")` 即可
- `wrap_eval_prompt_with_qwen_template()`（第 97–122 行）—— 内联到需要的地方或直接删除（若 tokenizer  apply_chat_template 在 `batch_inference` 中使用）
- `batch_inference()`（第 167–208 行）与 `batch_inference_with_reasoning()`（第 125–165 行）—— 合并为一个私有方法 `_batch_inference(samples, enable_thinking)`，减少重复代码
- `evaluate_one_sample_by_api()` / `evaluate_batch_samples_by_api()` —— Router 层直接逐条调用 `evaluate_one_sample` 即可；Math 的 API 路径下 `evaluate_one_sample` 可直接走 API 生成逻辑
- `evaluate_batch_samples()` —— 保留但精简，删除 API 分支的特殊处理，改为统一逻辑

**保留内容**：
- `__init__`：初始化模型或 LLMProvider，参数从 dict 读取
- `evaluate_one_sample`：单条评估（本地模型或 API）
- `evaluate_batch_samples`：批量评估（供 Router 可选使用）
- `_is_answer_correct_logic`、`_extract_final_answer_from_text`：核心答案提取与比对逻辑保留
- `load_model_and_tokenizer`：从 BaseEvaluator 继承即可

**改造后核心结构**：

```python
class MathEvaluator(BaseEvaluator):
    def __init__(self, config: Optional[Dict[str, Any]] = None):
        super().__init__(config)
        self._use_api = bool(self.config.get("_use_vllm_api", False))
        if self._use_api:
            self.model = None
            self.tokenizer = None
            self._llm_provider = LLMProvider(
                provider="vllm",
                model_name=str(self.config.get("vllm_model_name", "")),
                base_url=str(self.config.get("vllm_base_url", "")),
                api_key=str(self.config.get("vllm_api_key", "")),
                max_tokens=int(self.config.get("max_new_tokens", 1024)),
                temperature=float(self.config.get("temperature", 0.0)),
            )
        else:
            self.load_model_and_tokenizer()

    def evaluate_one_sample(self, sample: Dict[str, Any]) -> Dict[str, float]:
        if self._use_api:
            return self._evaluate_one_sample_api(sample)
        return self.evaluate_batch_samples([sample])

    def evaluate_batch_samples(self, samples: List[Dict[str, Any]]) -> Dict[str, float]:
        # 精简后的统一逻辑：本地模型批量推理 或 API 逐条生成
        ...

    def _evaluate_one_sample_api(self, sample: Dict[str, Any]) -> Dict[str, float]:
        prompt = sample.get(self.config.get("prompt_key", "prompt"), "")
        label_raw = sample.get(self.config.get("ground_true_answer_key", "answer"), "")
        gen_text = self._llm_provider.generate(
            [{"role": "user", "content": prompt}],
            max_tokens=int(self.config.get("max_new_tokens", 1024)),
            temperature=float(self.config.get("temperature", 0.0)),
        )
        pred_answer = self._extract_final_answer_from_text(gen_text)
        std_answer = self._extract_final_answer_from_text(str(label_raw))
        ok = self._is_answer_correct_logic(pred_answer, std_answer)
        return {
            "math_accuracy": 1.0 if ok else 0.0,
            "accuracy": 1.0 if ok else 0.0,
            "exact_match": 1.0 if ok else 0.0,
            "total_samples": 1,
            "correct_predictions": float(ok),
        }

    def _is_answer_correct_logic(self, predicted_answer: str, standard_answer: str) -> bool:
        # 原逻辑保留
        ...

    def _extract_final_answer_from_text(self, text: str) -> str:
        # 原逻辑保留
        ...
```

### 4.5 `bleu_rouge_evaluator.py` 与 `medqa_llm_evaluator.py` 微调

这两个 evaluator 已经比较精简，但仍需：
1. **删除 `run()` 函数** 及 `if __name__ == "__main__":`
2. **删除 `save_results()`**（BleuRouge 中的第 210–214 行）—— Router 层已负责返回结果
3. **去除 `settings` 引用**：目前这两个文件没有直接 import settings（通过 BaseEvaluator 间接使用），BaseEvaluator 改造后自然解决
4. **移动文件路径**：`src/evaluation/bleu_rouge_evaluator.py` → `src/utils/bleu_rouge_evaluator.py`
5. **更新内部 import**：例如 `from evaluation.base_evaluator import BaseEvaluator` → `from utils.base_evaluator import BaseEvaluator`

---

## 5. API Router 改造方案

### 5.1 核心思路

`evaluation.py` 不再依赖 `async_eval_queue` 中的 `evaluation_runner.execute_evaluation_task`，而是：
1. 自行加载 JSON 数据集。
2. 根据 `task_type` 直接 `import` 对应的 evaluator 类并实例化。
3. 在线程池中逐条调用 `evaluate_one_sample()`。
4. 按任务类型聚合指标。
5. 更新内存任务表。

### 5.2 `async_eval_queue.py` 的改造

当前 `async_eval_queue.py` 第 21 行 `from evaluation.evaluation_runner import execute_evaluation_task` 必须移除。改造后，`_worker_loop` 不再内联执行 runner，而是改为接收一个可调用对象（由 Router 注入），或者更简单地——**直接在 Router 的 endpoint 中使用 `asyncio.to_thread` 执行，不再使用队列 worker 模式**。

考虑到用户要求"简化"，推荐方案：**保留 `async_eval_queue.py` 的队列机制和内存表，但将 `execute_evaluation_task` 替换为 Router 层传入的函数**。这样改动最小，且仍保持异步非阻塞特性。

改造后的 `async_eval_queue.py` 核心逻辑：

```python
"""评估任务异步队列与内存结果表。"""

from __future__ import annotations

import asyncio
import traceback
from typing import Any, Dict, Optional, Callable

from utils.logger import setup_logger

logger = setup_logger(__name__, level="INFO")

evaluation_task_queue: asyncio.Queue = asyncio.Queue()
EVALUATION_TASKS: Dict[str, Dict[str, Any]] = {}
_worker_task: Optional[asyncio.Task] = None

# 由 Router 在启动时注入
_evaluation_task_fn: Optional[Callable[[Dict[str, Any]], Dict[str, Any]]] = None


def set_evaluation_task_fn(fn: Callable[[Dict[str, Any]], Dict[str, Any]]) -> None:
    global _evaluation_task_fn
    _evaluation_task_fn = fn


async def _worker_loop() -> None:
    while True:
        payload: Dict[str, Any] = await evaluation_task_queue.get()
        tid: str = payload["evaluation_task_id"]
        try:
            entry = EVALUATION_TASKS.get(tid)
            if entry is None:
                continue
            entry["status"] = "running"
            if _evaluation_task_fn is None:
                raise RuntimeError("evaluation_task_fn 未注册")
            result = await asyncio.to_thread(_evaluation_task_fn, payload)
            entry["metrics"] = result.get("metrics", {})
            entry["process_time"] = result.get("process_time", 0.0)
            entry["additional_info"] = {**(entry.get("additional_info") or {}), **(result.get("additional_info") or {})}
            entry["model_name"] = result.get("model_name", entry.get("model_name", ""))
            entry["status"] = "stop"
        except asyncio.CancelledError:
            break
        except Exception as e:
            logger.error("评估任务失败: %s\n%s", e, traceback.format_exc())
            if tid in EVALUATION_TASKS:
                EVALUATION_TASKS[tid]["status"] = "stop"
                EVALUATION_TASKS[tid]["additional_info"] = {
                    **(EVALUATION_TASKS[tid].get("additional_info") or {}),
                    "error": str(e),
                }
        finally:
            evaluation_task_queue.task_done()


# start_evaluation_worker / stop_evaluation_worker / register_pending_task / get_task_snapshot 保持不变
```

### 5.3 `evaluation.py` Router 重写

```python
"""
评估服务路由：直接实例化 evaluator，在线程池中运行，聚合指标。
"""

from __future__ import annotations

import json
import os
import time
import uuid
from enum import Enum
from pathlib import Path
from typing import Any, Dict, List, Optional

from fastapi import APIRouter, HTTPException
from pydantic import BaseModel, Field

from utils.async_eval_queue import (
    evaluation_task_queue,
    get_task_snapshot,
    register_pending_task,
    set_evaluation_task_fn,
    start_evaluation_worker,
)
from utils.inference_utils import display_model_name, is_valid_vllm_triple
from utils.task_types import EvaluationTaskType

router = APIRouter()


# ---------------------------------------------------------------------------
# Pydantic Models（见第 3.2 节）
# ---------------------------------------------------------------------------
class EvaluatorConfig(BaseModel):
    model_name_or_path: Optional[str] = Field(default=None)
    device: str = Field(default="cpu")
    max_new_tokens: int = Field(default=2048)
    temperature: float = Field(default=0.7)
    top_p: float = Field(default=0.9)
    vllm_model_name: Optional[str] = Field(default=None)
    vllm_base_url: Optional[str] = Field(default=None)
    vllm_api_key: Optional[str] = Field(default=None)
    prompt_key: str = Field(default="prompt")
    ground_true_answer_key: str = Field(default="answer")
    padding_side: str = Field(default="left")
    use_fast: bool = Field(default=True)
    enable_thinking: bool = Field(default=False)
    max_length: int = Field(default=2048)
    beta: float = Field(default=0.1)
    query_key: str = Field(default="query")
    chosen_key: str = Field(default="chosen")
    rejected_key: str = Field(default="rejected")
    execution_timeout: int = Field(default=30)
    max_memory_mb: int = Field(default=256)
    sandbox_working_dir: str = Field(default="/tmp/code_eval")
    enable_security_check: bool = Field(default=True)


class EvaluationRequest(BaseModel):
    evaluation_task_id: Optional[str] = Field(default=None)
    task_type: EvaluationTaskType
    dataset_path: str = Field(...)
    question_key: str = Field(default="question")
    model_answer_key: str = Field(default="output")
    ground_true_answer_key: str = Field(default="answer")
    evaluator_config: EvaluatorConfig = Field(default_factory=EvaluatorConfig)


class EvaluationResponse(BaseModel):
    evaluation_task_id: str
    metrics: Dict[str, float] = Field(default_factory=dict)
    model_name: str = ""
    status: str = "pending"
    process_time: float = 0.0
    additional_info: Optional[Dict[str, Any]] = None


# ---------------------------------------------------------------------------
# 数据集加载与规范化
# ---------------------------------------------------------------------------
def _load_dataset(path: str) -> List[Dict[str, Any]]:
    p = Path(path)
    if not p.is_file():
        raise FileNotFoundError(f"数据集文件不存在: {path}")
    if not str(path).lower().endswith(".json"):
        raise ValueError("仅支持 .json 数据集")
    with open(p, "r", encoding="utf-8") as f:
        data = json.load(f)
    if not isinstance(data, list):
        raise ValueError("数据集 JSON 必须为对象数组")
    return data


def _normalize_rows(
    rows: List[Dict[str, Any]],
    question_key: str,
    model_answer_key: str,
    ground_true_answer_key: str,
) -> List[Dict[str, Any]]:
    out: List[Dict[str, Any]] = []
    for row in rows:
        merged = dict(row)
        merged["question"] = row.get(question_key, "")
        merged["output"] = row.get(model_answer_key, "")
        merged["answer"] = row.get(ground_true_answer_key, "")
        merged["prompt"] = merged["question"]
        merged["code"] = merged["output"]
        out.append(merged)
    return out


# ---------------------------------------------------------------------------
# 聚合函数（原 evaluation_runner.py 中的聚合逻辑）
# ---------------------------------------------------------------------------
def _aggregate_medqa(rows: List[Dict[str, Any]]) -> Dict[str, float]:
    if not rows:
        return {}
    acc = sum(1 for r in rows if r.get("is_correct")) / len(rows)
    return {"accuracy": float(acc), "num_samples": float(len(rows))}


def _aggregate_bleu(rows: List[Dict[str, Any]]) -> Dict[str, float]:
    if not rows:
        return {}
    return {
        "bleu": float(sum(r.get("bleu", 0) for r in rows) / len(rows)),
        "rougeL": float(sum(r.get("rouge", 0) for r in rows) / len(rows)),
        "perplexity": float(sum(r.get("perplexity", 0) for r in rows) / len(rows)),
    }


def _aggregate_math(rows: List[Dict[str, Any]]) -> Dict[str, float]:
    if not rows:
        return {}
    acc = sum(float(r.get("math_accuracy", 0)) for r in rows) / len(rows)
    return {"math_accuracy": float(acc), "num_samples": float(len(rows))}


def _aggregate_code(rows: List[Dict[str, Any]]) -> Dict[str, float]:
    if not rows:
        return {}
    passed = sum(1 for r in rows if r.get("success"))
    return {
        "sample_pass_rate": float(passed / len(rows)),
        "mean_pass_rate": float(sum(float(r.get("pass_rate", 0)) for r in rows) / len(rows)),
        "num_samples": float(len(rows)),
    }


def _aggregate_dpo(rows: List[Dict[str, Any]]) -> Dict[str, float]:
    if not rows:
        return {}
    out: Dict[str, float] = {"num_samples": float(len(rows))}
    for k in rows[0].keys():
        vals = [float(r[k]) for r in rows if k in r and isinstance(r[k], (int, float))]
        if vals:
            out[k] = sum(vals) / len(vals)
    return out


# ---------------------------------------------------------------------------
# Evaluator 映射（原 EvaluatorFactory._MAP 的内联化）
# ---------------------------------------------------------------------------
def _get_evaluator_class(task_type: EvaluationTaskType):
    # 延迟导入，避免循环依赖
    if task_type == EvaluationTaskType.MEDQA_LLM:
        from utils.medqa_llm_evaluator import MedQALLMEvaluator
        return MedQALLMEvaluator
    elif task_type == EvaluationTaskType.BLEU_ROUGE:
        from utils.bleu_rouge_evaluator import BleuRougeEvaluator
        return BleuRougeEvaluator
    # 以下三项已删除（见补充方案二）
    # elif task_type == EvaluationTaskType.MATH:
    #     from utils.math_evaluator import MathEvaluator
    #     return MathEvaluator
    # elif task_type == EvaluationTaskType.CODE:
    #     from utils.code_evaluator import CodeEvaluator
    #     return CodeEvaluator
    # elif task_type == EvaluationTaskType.DPO_QUALITY:
    #     from utils.dpo_quality_evaluator import DPOQualityEvaluator
    #     return DPOQualityEvaluator
    else:
        raise ValueError(f"不支持的评估任务类型: {task_type}")


# ---------------------------------------------------------------------------
# 核心执行函数（替代原 evaluation_runner.execute_evaluation_task）
# ---------------------------------------------------------------------------
def execute_evaluation_task(payload: Dict[str, Any]) -> Dict[str, Any]:
    project_root = Path(__file__).resolve().parent.parent.parent.parent
    prev_cwd = Path.cwd()
    os.chdir(project_root)
    t0 = time.time()

    task_type = EvaluationTaskType(payload["task_type"])
    dataset_path = payload["dataset_path"]
    question_key = payload["question_key"]
    model_answer_key = payload["model_answer_key"]
    ground_true_answer_key = payload["ground_true_answer_key"]
    eval_cfg = payload["evaluator_config"]  # dict

    raw = _load_dataset(dataset_path)
    samples = _normalize_rows(raw, question_key, model_answer_key, ground_true_answer_key)

    vllm_ok = is_valid_vllm_triple(
        eval_cfg.get("vllm_model_name"),
        eval_cfg.get("vllm_base_url"),
        eval_cfg.get("vllm_api_key"),
    )
    model_path = (eval_cfg.get("model_name_or_path") or "").strip()
    if not vllm_ok and not model_path:
        raise ValueError("vLLM 配置无效且未提供 model_name_or_path，无法评估")

    # 组装传给 evaluator 的 config dict
    config = dict(eval_cfg)
    config["_vllm_config_valid"] = vllm_ok
    config["_use_vllm_api"] = vllm_ok
    if vllm_ok and not model_path:
        config["model_name_or_path"] = "__vllm_only__"

    # 环境变量覆盖 max_new_tokens
    _mt = os.environ.get("EVAL_MAX_NEW_TOKENS")
    if _mt and str(_mt).strip().isdigit():
        config["max_new_tokens"] = int(_mt)

    # 特殊字段映射（保持与 runner 兼容）
    if task_type == EvaluationTaskType.DPO_QUALITY:
        config["query_key"] = "question"
        config["chosen_key"] = "output"
        config["rejected_key"] = "answer"

    ev = None
    additional: Dict[str, Any] = {"num_rows": len(samples), "use_vllm_api": vllm_ok}
    metrics: Dict[str, float] = {}

    try:
        eval_cls = _get_evaluator_class(task_type)
        ev = eval_cls(config)
        rows_out = [ev.evaluate_one_sample(s) for s in samples]

        if task_type == EvaluationTaskType.MEDQA_LLM:
            metrics = _aggregate_medqa(rows_out)
        elif task_type == EvaluationTaskType.BLEU_ROUGE:
            metrics = _aggregate_bleu(rows_out)
        elif task_type == EvaluationTaskType.MATH:
            metrics = _aggregate_math(rows_out)
        elif task_type == EvaluationTaskType.CODE:
            metrics = _aggregate_code(rows_out)
        elif task_type == EvaluationTaskType.DPO_QUALITY:
            metrics = _aggregate_dpo(rows_out)
    finally:
        if ev is not None and hasattr(ev, "cleanup") and callable(getattr(ev, "cleanup")):
            try:
                ev.cleanup()
            except Exception:
                pass
        os.chdir(prev_cwd)

    elapsed = time.time() - t0
    mname = display_model_name(
        vllm_valid=vllm_ok,
        model_name=eval_cfg.get("vllm_model_name"),
        model_path=model_path,
    )
    return {
        "metrics": metrics,
        "process_time": elapsed,
        "additional_info": additional,
        "model_name": mname,
    }


# ---------------------------------------------------------------------------
# 启动时向 async_eval_queue 注册执行函数
# ---------------------------------------------------------------------------
set_evaluation_task_fn(execute_evaluation_task)


# ---------------------------------------------------------------------------
# HTTP Endpoints
# ---------------------------------------------------------------------------
@router.post("/evaluate_model", response_model=EvaluationResponse)
async def evaluate_model(request: EvaluationRequest) -> EvaluationResponse:
    await start_evaluation_worker()
    task_id = (request.evaluation_task_id or "").strip() or str(uuid.uuid4())

    vllm_ok = is_valid_vllm_triple(
        request.evaluator_config.vllm_model_name,
        request.evaluator_config.vllm_base_url,
        request.evaluator_config.vllm_api_key,
    )
    display_name = display_model_name(
        vllm_valid=vllm_ok,
        model_name=request.evaluator_config.vllm_model_name,
        model_path=request.evaluator_config.model_name_or_path or "",
    )
    register_pending_task(task_id, display_name)

    await evaluation_task_queue.put({
        "evaluation_task_id": task_id,
        "task_type": request.task_type.value,
        "dataset_path": request.dataset_path,
        "question_key": request.question_key,
        "model_answer_key": request.model_answer_key,
        "ground_true_answer_key": request.ground_true_answer_key,
        "evaluator_config": request.evaluator_config.model_dump(),
    })

    snap = get_task_snapshot(task_id) or {}
    return EvaluationResponse(
        evaluation_task_id=task_id,
        metrics=snap.get("metrics") or {},
        model_name=str(snap.get("model_name") or display_name),
        status=snap.get("status", "pending"),
        process_time=float(snap.get("process_time") or 0.0),
        additional_info=snap.get("additional_info"),
    )


@router.get("/get_evaluation_result/{evaluation_task_id}", response_model=EvaluationResponse)
async def get_evaluation_result(evaluation_task_id: str) -> EvaluationResponse:
    snap = get_task_snapshot(evaluation_task_id)
    if snap is None:
        raise HTTPException(status_code=404, detail="未知的 evaluation_task_id")
    return EvaluationResponse(
        evaluation_task_id=evaluation_task_id,
        metrics=snap.get("metrics") or {},
        model_name=str(snap.get("model_name") or ""),
        status=snap["status"],
        process_time=float(snap.get("process_time") or 0.0),
        additional_info=snap.get("additional_info"),
    )
```

---

## 6. 工厂与 Runner 的处置

### 6.1 `evaluation_runner.py`

- **处置方式**：**直接删除**。
- **代码去向**：
  - `_load_dataset()` → 内联到 `evaluation.py` Router（`_load_dataset` + `_normalize_rows`）
  - `_build_config()` → 废弃；config 合并逻辑不再需要，Router 直接组装 dict
  - `_aggregate_*()` → 内联到 `evaluation.py` Router
  - `execute_evaluation_task()` → 重写后整体迁入 `evaluation.py` Router 模块级函数

### 6.2 `evaluator_factory.py`

- **处置方式**：**直接删除**。
- **代码去向**：
  - `EvaluatorFactory._MAP` → 内联为 `evaluation.py` 中的 `_get_evaluator_class()` 延迟导入函数
  - `EvaluatorFactory.create()` → 废弃；Router 直接调用 `_get_evaluator_class(task_type)(config)`
  - `EvaluatorFactory.merge_config()` → 废弃；OmegaConf 合并不再需要，Python dict 直接覆盖即可

### 6.3 `async_eval_queue.py`

- **处置方式**：**移动到 `src/utils/async_eval_queue.py`，并修改内部 import**。
- **改造点**：
  - 第 21 行 `from evaluation.evaluation_runner import execute_evaluation_task` 删除
  - 新增 `set_evaluation_task_fn()` 注册机制（见 5.2 节）
  - 其余 `start_evaluation_worker`、`stop_evaluation_worker`、`register_pending_task`、`get_task_snapshot` 保持不变

### 6.4 `inference_utils.py` 与 `task_types.py`

- **处置方式**：**移动到 `src/utils/`**。
- **改造点**：
  - `inference_utils.py`：内容不变，仅文件位置变更
  - `task_types.py`：内容不变，仅文件位置变更；Router 中继续导入 `EvaluationTaskType`

---

## 7. 依赖关系修复清单

### 7.1 `src/api/routers/evaluation.py`

| 行号 | 修改前 | 修改后 |
|---|---|---|
| 14–19 | `from evaluation.async_eval_queue import ...` | `from utils.async_eval_queue import ...` |
| 20 | `from evaluation.inference_utils import ...` | `from utils.inference_utils import ...` |
| 21 | `from evaluation.task_types import EvaluationTaskType` | `from utils.task_types import EvaluationTaskType` |
| 32–52 | `EvaluationRequest` / `EvaluationResponse` | 按 3.2 节重写，增加 `EvaluatorConfig` |
| 54–93 | `evaluate_model` endpoint | 按 5.3 节重写 |
| 96–112 | `get_evaluation_result` endpoint | 保持不变（Response 模型字段需同步） |

### 7.2 `src/api/main.py`

| 行号 | 修改前 | 修改后 |
|---|---|---|
| 25 | `from evaluation.async_eval_queue import start_evaluation_worker, stop_evaluation_worker` | `from utils.async_eval_queue import start_evaluation_worker, stop_evaluation_worker` |

### 7.3 `src/api/eval_minimal_main.py`

| 行号 | 修改前 | 修改后 |
|---|---|---|
| 14 | `from evaluation.async_eval_queue import start_evaluation_worker, stop_evaluation_worker` | `from utils.async_eval_queue import start_evaluation_worker, stop_evaluation_worker` |
| 15 | `from routers import evaluation` | 保持不变 |

### 7.4 `src/evaluation/__init__.py`

| 行号 | 修改前 | 修改后 |
|---|---|---|
| 1–6 | 导出 `EvaluatorFactory`、`EvaluationTaskType` | 清空为仅 docstring，或删除文件（若目录清空后可删除） |

### 7.5 所有 `*_evaluator.py` 的 import 修正

每个 evaluator 移动到 `src/utils/` 后，需统一修改内部 import：

| 文件 | 修改前 | 修改后 |
|---|---|---|
| `base_evaluator.py` | `from utils.logger import setup_logger`<br>`from config.settings import settings` | `from utils.logger import setup_logger`<br>（删除 settings 导入） |
| `bleu_rouge_evaluator.py` | `from evaluation.base_evaluator import BaseEvaluator, EvaluatorDataset` | `from utils.base_evaluator import BaseEvaluator, EvaluatorDataset` |
| `medqa_llm_evaluator.py` | `from evaluation.base_evaluator import BaseEvaluator, EvaluatorDataset` | `from utils.base_evaluator import BaseEvaluator, EvaluatorDataset` |
| ~~`code_evaluator.py`~~ | **【直接删除，见补充方案二】** | — |
| ~~`math_evaluator.py`~~ | **【直接删除，见补充方案二】** | — |
| ~~`dpo_quality_evaluator.py`~~ | **【直接删除，见补充方案二】** | — |

---

## 8. 目录结构终态

### 8.1 `src/evaluation/`（收缩后）

```
src/evaluation/
└── __init__.py          # 空文件，或整体删除该目录
```

> 若将 `__init__.py` 清空后，`src/evaluation/` 目录本身可以保留空壳（不影响 Python import），也可以完全删除。建议**完全删除**该目录，避免后续开发者误用。

### 8.2 `src/utils/`（扩充后）

```
src/utils/
├── __init__.py
├── async_eval_queue.py         ← 原 src/evaluation/async_eval_queue.py
├── base_evaluator.py           ← 原 src/evaluation/base_evaluator.py
├── bleu_rouge_evaluator.py     ← 原 src/evaluation/bleu_rouge_evaluator.py
# code_evaluator.py 已删除（见补充方案二）
# dpo_quality_evaluator.py 已删除（见补充方案二）
├── file_utils.py
├── gspo_utils.py
├── inference_utils.py          ← 原 src/evaluation/inference_utils.py
├── json_parse_utils.py
├── logger.py
# math_evaluator.py 已删除（见补充方案二）
├── medqa_llm_evaluator.py      ← 原 src/evaluation/medqa_llm_evaluator.py
├── metrics.py
├── task_types.py               ← 原 src/evaluation/task_types.py
├── text_utils.py
└── profiler/
```

---

## 9. 实施步骤（建议执行顺序）

按以下顺序执行，可最大限度避免循环依赖和编译错误。

### 第 1 步：移动无依赖的辅助文件
1. 将 `src/evaluation/inference_utils.py` → `src/utils/inference_utils.py`
2. 将 `src/evaluation/task_types.py` → `src/utils/task_types.py`
3. 此时两个文件内容不变，仅变更路径。

### 第 2 步：移动并改造 `base_evaluator.py`
1. 将 `src/evaluation/base_evaluator.py` → `src/utils/base_evaluator.py`
2. 删除 `from config.settings import settings`
3. 修改 `__init__`：不再自动读取 settings，不再自动构建 DataLoader（或仅在有数据时构建）
4. 验证：`python -c "from utils.base_evaluator import BaseEvaluator; print('ok')"`

### 第 3 步：移动并改造三个保留 evaluator
1. `bleu_rouge_evaluator.py` → `src/utils/bleu_rouge_evaluator.py`
   - 修改 import 路径
   - 删除 `run()` 和 `save_results()`
2. `medqa_llm_evaluator.py` → `src/utils/medqa_llm_evaluator.py`
   - 修改 import 路径
   - 删除 `run()`
3. 分别验证语法：`python -m py_compile src/utils/bleu_rouge_evaluator.py`

### 第 4 步：移动并简化三个核心 evaluator
1. ~~`code_evaluator.py` → `src/utils/code_evaluator.py`~~ **【作废，直接删除】**
   - 根据补充方案二，`code_evaluator.py` 直接删除，不再移动或简化。
2. ~~`math_evaluator.py` → `src/utils/math_evaluator.py`~~ **【作废，直接删除】**
   - 根据补充方案二，`math_evaluator.py` 直接删除，不再移动或简化。
3. ~~`dpo_quality_evaluator.py` → `src/utils/dpo_quality_evaluator.py`~~ **【作废，直接删除】**
   - 根据补充方案二，`dpo_quality_evaluator.py` 直接删除，不再移动或简化。

### 第 5 步：改造 `async_eval_queue.py` 并移动
1. `async_eval_queue.py` → `src/utils/async_eval_queue.py`
2. 删除 `from evaluation.evaluation_runner import execute_evaluation_task`
3. 添加 `set_evaluation_task_fn()` 注册机制
4. 验证语法。

### 第 6 步：删除 runner 和工厂
1. 删除 `src/evaluation/evaluation_runner.py`
2. 删除 `src/evaluation/evaluator_factory.py`
3. 删除 `src/evaluation/data/`、`src/evaluation/logs/`、`src/evaluation/run_evaluator/`

### 第 7 步：重写 `src/api/routers/evaluation.py`
1. 按 5.3 节完整重写 Router
2. 更新所有 import 路径
3. 确保 `EvaluationRequest` 包含 `EvaluatorConfig`

### 第 8 步：修复 API 入口 import
1. `src/api/main.py`：更新 `async_eval_queue` 的 import 路径
2. `src/api/eval_minimal_main.py`：更新 `async_eval_queue` 的 import 路径

### 第 9 步：清理 config.yaml
1. 删除 `src/config/config.yaml` 第 104–131 行 `evaluator:` 节
2. 检查 `src/config/settings.py` 中是否有仅被 evaluator 使用的属性；若有，标记为可删除（但本次方案暂不强制修改 settings.py，因其他模块可能依赖）

### 第 10 步：清理 `src/evaluation/` 目录
1. 清空或删除 `src/evaluation/__init__.py`
2. 若目录为空，删除 `src/evaluation/` 目录

### 第 11 步：端到端验证
1. 启动 API：`cd src/api && python main.py`
2. 发送测试请求到 `/api/evaluation/evaluate_model`
3. 检查 `/api/evaluation/get_evaluation_result/{task_id}` 返回正常
4. 分别测试 MEDQA_LLM、BLEU_ROUGE、MATH、CODE、DPO_QUALITY 五种任务类型

---

## 10. 风险提示

| 风险点 | 影响 | 缓解措施 |
|---|---|---|
| **BaseEvaluator 不再自动加载数据集** | 若某个外部脚本仍直接实例化 evaluator 并调用 `.evaluate()`，会因 `test_data` 为空而报错 | 搜索全仓库对 `BleuRougeEvaluator()`、`MedQALLMEvaluator()` 等直接实例化并调用 `.evaluate()` 的代码；本次调查已确认无此类外部调用（`tests/`、`scripts/` 未发现），风险低 |
| **config 从 OmegaConf DictConfig 变为 Python dict** | 原代码中大量 `self.config.xxx` 需改为 `self.config.get("xxx", default)` | 在简化 evaluator 时统一替换；BleuRouge / MedQA LLM 也需同步调整少量字段访问方式（如 `self.config.prompt_key` → `self.config.get("prompt_key", "prompt")`） |
| **DPO / Math 本地模型路径依赖 `load_model_and_tokenizer`** | 若 `model_name_or_path` 传入空字符串，会触发 `AutoModelForCausalLM.from_pretrained("")` 报错 | Router 层已在 `execute_evaluation_task` 中校验：`if not vllm_ok and not model_path: raise ValueError(...)` |
| **删除 `evaluate()` 后失去批量遍历能力** | Router 层已接管遍历和聚合，不影响 API 使用；若有人直接 import evaluator 做离线批量评估，需改用循环调用 `evaluate_one_sample` | 在代码审查阶段确认无此类离线调用 |
| **async_eval_queue 的 worker 在进程生命周期内只启动一次** | 若 `set_evaluation_task_fn` 在 worker 启动后才被调用，理论上不影响，因为 payload 进入队列后 worker 才消费；但首次请求会触发 `start_evaluation_worker()` | 保持现有 `start_evaluation_worker` 调用逻辑不变 |
| **CodeExecutor 的 `__del__` 与临时目录清理** | `cleanup()` 实例方法被删除后，依赖 `__del__` 触发；若 Python 解释器退出时不保证调用 `__del__`，可能残留 `/tmp/code_exec_*` 目录 | 在 `CodeExecutor.__del__` 中保留清理逻辑；或在 Router 的 `finally` 块中调用 `ev.executor.cleanup()`（若保留 `ev.cleanup()` 存根） |
| **回滚策略** | 若改造后某 evaluator 行为异常 | 建议改造前执行 `git commit` 或备份当前 `src/evaluation/` 目录；出现问题时可逐文件回滚 |

---

## 附录：快速检查清单（Checklist）

- [ ] `src/evaluation/` 目录下仅剩空 `__init__.py` 或已整体删除
- [ ] `src/utils/` 下已包含全部 5 个 evaluator + `base_evaluator.py`
- [ ] 所有 `*_evaluator.py` 中无 `from config.settings import settings`
- [ ] 所有 `*_evaluator.py` 中无 `run()` / `run_evaluation()` / `if __name__ == "__main__":`
- [ ] `src/config/config.yaml` 中无 `evaluator:` 节
- [ ] `src/api/routers/evaluation.py` 可直接运行五种 evaluator 并聚合指标
- [ ] `python -m py_compile src/api/routers/evaluation.py` 通过
- [ ] `python -m py_compile src/utils/*.py` 全部通过
- [ ] API 启动无 ImportError
- [ ] 至少完成一种 task_type 的端到端调用测试

---

## 补充方案：全面禁止本地模型权重，强制使用 vLLM API

> **目标**：所有 evaluator（`BleuRougeEvaluator`、`MedQALLMEvaluator`、`MathEvaluator`、`DPOQualityEvaluator`、`CodeEvaluator`）**只能通过 vLLM / OpenAI 兼容 API**（`model_name` + `base_url` + `api_key`）进行评估，彻底删除本地 `transformers` 权重加载与推理路径。

---

### A.1 现状逐文件分析

#### A.1.1 `src/utils/base_evaluator.py`

| 位置 | 问题描述 |
|---|---|
| 第 36–55 行 | `load_model_and_tokenizer()` 方法使用 `AutoModelForCausalLM.from_pretrained` 和 `AutoTokenizer.from_pretrained` 加载本地权重。 |
| 影响范围 | `BleuRougeEvaluator`、`MathEvaluator`、`DPOQualityEvaluator` 均继承 `BaseEvaluator` 并直接或间接调用此方法。 |

#### A.1.2 `src/utils/bleu_rouge_evaluator.py`

| 行号 | 问题描述 |
|---|---|
| 第 12 行 | `from transformers import AutoModelForCausalLM, AutoTokenizer`（本地模型依赖） |
| 第 13 行 | `import torch`（本地推理依赖） |
| 第 52–73 行 | `else` 分支（`self._use_api == False`）中通过 `AutoModelForCausalLM.from_pretrained` 加载本地模型到 `self.device` |
| 第 124–172 行 | `evaluate_batch_sample` 方法的 `else` 分支（第 132 行起）使用 `self.tokenizer(...)` + `self.model.generate(...)` + `self.model(...)` 进行本地推理并计算 perplexity |
| 第 98–105 行 | `evaluate_one_sample` 在 `self._use_api == False` 时调用本地批量方法 `evaluate_batch_sample([sample])` |

#### A.1.3 ~~`src/utils/math_evaluator.py`~~ 【已作废，文件直接删除，见补充方案二】

| 行号 | 问题描述 |
|---|---|
| 第 11 行 | `from transformers import AutoModelForCausalLM, AutoTokenizer` |
| 第 12 行 | `import torch` |
| 第 35–36 行 | `else` 分支调用 `self.load_model_and_tokenizer()` |
| 第 38–53 行 | 覆写的 `load_model_and_tokenizer()` 使用 `AutoModelForCausalLM.from_pretrained` + `AutoTokenizer.from_pretrained` |
| 第 55–58 行 | `evaluate_one_sample` 在 `self._use_api == False` 时调用 `self.evaluate_batch_samples([sample])` |
| 第 60–138 行 | `evaluate_batch_samples` 的 `else` 分支（第 74 行起）使用 `self.tokenizer.apply_chat_template` + `self.tokenizer(...)` + `self.model.generate(...)` + `self.tokenizer.batch_decode(...)` 进行本地批量推理 |

#### A.1.4 ~~`src/utils/dpo_quality_evaluator.py`~~ 【已作废，文件直接删除，见补充方案二】

| 行号 | 问题描述 |
|---|---|
| 第 11 行 | `from transformers import PreTrainedModel, PreTrainedTokenizer, AutoModelForCausalLM, AutoTokenizer` |
| 第 12 行 | `import torch` |
| 第 48–71 行 | `else` 分支（`self._use_api == False`）使用 `AutoModelForCausalLM.from_pretrained` 加载主模型和参考模型到 `self.device`，并调用 `self.model.eval()` |
| 第 73–100 行 | `_prepare_dpo_batch()` 使用 `self.tokenizer(...)` 对 query / chosen / rejected 做 padding / tokenize，仅在本地模式有效 |
| 第 102–124 行 | `_compute_logps()` 调用 `model(input_ids=..., attention_mask=...)` 获取 logits，仅在本地模式有效 |
| 第 126–163 行 | `_compute_rewards()` 调用 `_compute_logps()` 计算 policy 和 reference 的 log probs，仅在本地模式有效 |
| 第 165–189 行 | `evaluate_one_sample` 在 `self._use_api == False` 时调用 `_prepare_dpo_batch` + `_compute_rewards` |

#### A.1.5 `src/utils/medqa_llm_evaluator.py`

| 行号 | 问题描述 |
|---|---|
| 第 18 行 | docstring 声明支持"本地 transformers 权重" |
| 第 43–55 行 | `else` 分支中当 `vllm_ok == False` 时，使用 `LLMProvider(provider="local", local_backend="transformers", model_path=...)` 加载本地权重作为裁判模型 |

#### A.1.6 ~~`src/utils/code_evaluator.py`~~ 【已作废，文件直接删除，见补充方案二】

| 结论 | 说明 |
|---|---|
| 无需修改模型加载逻辑 | `CodeEvaluator` 本身不加载语言模型（仅执行沙箱代码）。但继承的 `BaseEvaluator.load_model_and_tokenizer` 应随基类一起删除。 |

#### A.1.7 `src/api/routers/evaluation.py`

| 行号 | 问题描述 |
|---|---|
| 第 193–195 行 | `if not vllm_ok and not model_path: raise ValueError(...)` 允许通过 `model_name_or_path`（本地路径）作为 fallback 评估方式。 |
| 第 197–201 行 | `config` 组装逻辑中，`model_path` 被用来设置 `config["model_name_or_path"]`，进一步传递给 evaluator 作为本地模型路径。 |

---

### A.2 改造方案

#### A.2.1 删除原则

1. **所有 `from transformers import ...` 在 evaluator 中彻底删除**。
2. **所有 `import torch` 在 evaluator 中彻底删除**（`CodeEvaluator` 除外，其不依赖 torch）。
3. **所有 `AutoModelForCausalLM.from_pretrained` / `AutoTokenizer.from_pretrained` 调用彻底删除**。
4. **所有 `self._use_api == False` 分支彻底删除**，不再保留双路径。
5. **Router 层不再允许 `model_name_or_path` 作为 fallback**；vLLM 三要素（`vllm_model_name` + `vllm_base_url` + `vllm_api_key`）为唯一合法评估方式。

#### A.2.2 各 evaluator 具体改造

##### 1) `BaseEvaluator`

- **删除** `load_model_and_tokenizer()` 方法（第 36–55 行）。
- 保留 `load_test_dataset()`（数据集加载与模型无关）。

##### 2) `BleuRougeEvaluator`

- **删除 import**：`from transformers import AutoModelForCausalLM, AutoTokenizer`、`import torch`
- **删除**第 52–73 行本地模型初始化 `else` 分支。
- **删除** `evaluate_batch_sample` 中的本地推理 `else` 分支（第 132–171 行）。
- **删除** `evaluate_one_sample` 中的 `if not self._use_api` 分支（第 101–105 行）。
- **保留**：`_llm_provider`（vLLM API）初始化、`evaluate_one_sample_by_api`、`_score_bleu_rouge_api_single`、BLEU / ROUGE 计算逻辑。
- **删除** `self._use_api` 字段，默认且唯一走 API 路径。

##### 3) `MathEvaluator`

- **删除 import**：`from transformers import AutoModelForCausalLM, AutoTokenizer`、`import torch`
- **删除**覆写的 `load_model_and_tokenizer()`（第 38–53 行）。
- **删除** `evaluate_batch_samples` 中的本地推理 `else` 分支（第 74–138 行）。
- **删除** `evaluate_one_sample` 中的 `if not self._use_api` 分支（第 55–58 行）。
- **保留**：`_llm_provider`（vLLM API）初始化、`_evaluate_one_sample_api`、`_is_answer_correct_logic`、`_extract_final_answer_from_text`。
- **删除** `self._use_api` 字段，默认且唯一走 API 路径。

##### 4) `DPOQualityEvaluator`

- **删除 import**：`from transformers import PreTrainedModel, PreTrainedTokenizer, AutoModelForCausalLM, AutoTokenizer`、`import torch`
- **删除**本地模型初始化 `else` 分支（第 48–71 行）。
- **删除** `_prepare_dpo_batch()`（第 73–100 行）。
- **删除** `_compute_logps()`（第 102–124 行）。
- **删除** `_compute_rewards()`（第 126–163 行）。
- **删除** `evaluate_one_sample` 中的本地路径分支（第 168–189 行）。
- **保留**：`_llm_provider`（vLLM API）初始化、`_evaluate_one_sample_api`（二值偏好近似）。
- **删除** `self._use_api`、`self.reference_model`、`self.reference_tokenizer`、`self.device` 等本地相关字段。

##### 5) `MedQALLMEvaluator`

- **修改 docstring**（第 18 行）：删除"本地 transformers 权重"声明，改为仅支持 vLLM / OpenAI 兼容 API。
- **删除** `_init_judge_provider` 中的 `else` 分支（第 43–55 行），即删除 `LLMProvider(provider="local", local_backend="transformers", ...)` 的 fallback。
- **修改** `vllm_ok == False` 时的行为：不再 fallback 到本地模型，而是直接抛出 `ValueError`，要求提供有效的 vLLM 配置。

##### 6) `CodeEvaluator`

- **无模型加载逻辑需要删除**，但需确认基类 `BaseEvaluator.load_model_and_tokenizer()` 删除后不影响编译。

#### A.2.3 `evaluation.py` Router 适配

| 行号 | 改造内容 |
|---|---|
| 第 193–195 行 | 将 `if not vllm_ok and not model_path: raise ValueError(...)` 改为 `if not vllm_ok: raise ValueError("评估必须提供有效的 vLLM 配置（vllm_model_name + vllm_base_url + vllm_api_key）")`。 |
| 第 197–201 行 | 删除 `model_path` 相关逻辑；不再组装 `config["model_name_or_path"]`；不再允许本地路径 fallback。 |
| `EvaluatorConfig` | 保留 `model_name_or_path` 字段（用于向后兼容或日志展示），但 Router 不再将其作为评估必需条件。 |

---

### A.3 删除清单汇总

| 文件 | 待删除内容 | 保留内容 |
|---|---|---|
| `base_evaluator.py` | `load_model_and_tokenizer()`（第 36–55 行） | `load_test_dataset()`、抽象方法定义 |
| `bleu_rouge_evaluator.py` | `transformers` / `torch` import；本地模型初始化 else 分支（第 52–73 行）；本地推理分支（第 101–105 行、第 132–171 行） | vLLM API 初始化；`_score_bleu_rouge_api_single`；BLEU / ROUGE 计算 |
| ~~`math_evaluator.py`~~ | **【直接删除，见补充方案二】** 原分析：transformers/torch import、load_model_and_tokenizer、本地推理分支等 | **文件已删除，无需改造** |
| ~~`dpo_quality_evaluator.py`~~ | **【直接删除，见补充方案二】** 原分析：transformers/torch import、本地模型初始化、_prepare_dpo_batch、_compute_logps、_compute_rewards 等 | **文件已删除，无需改造** |
| `medqa_llm_evaluator.py` | docstring 中本地权重声明；`_init_judge_provider` 的 local backend fallback（第 43–55 行） | vLLM API 初始化；`format_prompt`；`evaluate_one_sample` |
| `evaluation.py` | `model_path` 作为 fallback 的校验逻辑（第 193–201 行） | vLLM 三要素校验；Evaluator 映射；指标聚合 |

---

### A.4 实施步骤

按以下顺序执行，确保 evaluator 在任意时刻均不残留本地模型路径：

1. **改造 `BaseEvaluator`**
   - 删除 `load_model_and_tokenizer()` 方法。
   - 验证：`python -m py_compile src/utils/base_evaluator.py`

2. **改造 `BleuRougeEvaluator`**
   - 删除 `transformers` / `torch` import 及所有本地模型分支。
   - 删除 `self._use_api` 判断，统一走 `_llm_provider` API 路径。
   - 验证：`python -m py_compile src/utils/bleu_rouge_evaluator.py`

3. ~~**改造 `MathEvaluator`**~~ **【作废，见补充方案二】**
   - `math_evaluator.py` 已直接删除，无需改造。

4. ~~**改造 `DPOQualityEvaluator`**~~ **【作废，见补充方案二】**
   - `dpo_quality_evaluator.py` 已直接删除，无需改造。

5. **改造 `MedQALLMEvaluator`**
   - 修改 docstring，删除本地权重声明。
   - 删除 `_init_judge_provider` 中的 `LLMProvider(provider="local", ...)` fallback。
   - `vllm_ok == False` 时直接 `raise ValueError`。
   - 验证：`python -m py_compile src/utils/medqa_llm_evaluator.py`

6. **改造 `evaluation.py` Router**
   - 删除 `model_path` fallback 逻辑，仅校验 `vllm_ok`。
   - 验证：`python -m py_compile src/api/routers/evaluation.py`

7. **端到端验证**
   - 启动 API：`cd src/api && python main.py`
   - 调用 `/api/evaluation/evaluate_model`，确保五种 task_type 均只能通过有效 vLLM 配置执行；不提供 vLLM 配置时应返回明确的 `ValueError`。

---

### A.5 风险提示

| 风险点 | 影响 | 缓解措施 |
|---|---|---|
| **删除 `BaseEvaluator.load_model_and_tokenizer` 后，若外部脚本直接调用会报错** | 低；本次调查确认无外部直接调用 | 全局搜索 `load_model_and_tokenizer` 调用点，确保仅在 evaluator 内部使用 |
| **DPO 本地路径删除后失去精确 log prob 计算能力** | 中；API 模式仅能做二值偏好近似，无法输出 reward margin | 在 `_evaluate_one_sample_api` 返回的字段中，将 `chosen_rewards_mean` / `rejected_rewards_mean` / `rewards_margin_mean` 固定为 `0.0`，并在日志中说明近似原因 |
| **BleuRouge 本地路径删除后无法计算 perplexity** | 低；API 模式原已返回 `perplexity: 0.0` | 保持现状；若业务后续需要 perplexity，可通过 API 返回的 logprobs 近似计算（需 LLMProvider 支持） |
| **MedQA LLM 删除本地 fallback 后，离线无 GPU 环境无法评估** | 低；vLLM 服务可部署在独立机器 | 确保测试环境有可用的 vLLM endpoint |
| **Router 删除 `model_path` fallback 后，旧请求可能失败** | 中；旧请求若只传 `model_name_or_path` 不传 vLLM 三要素 | 返回明确的 `ValueError` 提示：`评估必须提供有效的 vLLM 配置（vllm_model_name + vllm_base_url + vllm_api_key）`，引导调用方修改请求参数 |

---

### A.6 快速检查清单（Checklist）

- [ ] `src/utils/base_evaluator.py` 中已删除 `load_model_and_tokenizer()`
- [ ] `src/utils/bleu_rouge_evaluator.py` 中无 `from transformers import ...`、无 `import torch`、无本地模型分支
- [ ] ~~`src/utils/math_evaluator.py`~~ **已删除**（见补充方案二）
- [ ] ~~`src/utils/dpo_quality_evaluator.py`~~ **已删除**（见补充方案二）
- [ ] `src/utils/medqa_llm_evaluator.py` docstring 中无"本地 transformers 权重"声明，且 `_init_judge_provider` 无 local backend fallback
- [ ] `src/api/routers/evaluation.py` 中删除 `model_path` fallback，仅允许 vLLM 配置通过
- [ ] `python -m py_compile src/utils/*.py` 全部通过
- [ ] `python -m py_compile src/api/routers/evaluation.py` 通过
- [ ] API 启动无 ImportError
- [ ] 不提供 vLLM 配置时，请求返回明确的 `ValueError`

---

## 补充方案二：code / math / dpo 三个 evaluator 直接删除

> **决定**：原方案中计划"简化保留"的 `code_evaluator.py`、`math_evaluator.py`、`dpo_quality_evaluator.py` **改为直接删除**，不再保留其任何代码。
>
> 保留的 evaluator 仅余：**`BleuRougeEvaluator`**、**`MedQALLMEvaluator`**、**`BaseEvaluator`**（基类）。

---

### B.1 删除范围

| 文件（当前路径） | 处置方式 | 说明 |
|---|---|---|
| `src/utils/code_evaluator.py` | **直接删除** | 不再保留任何代码 |
| `src/utils/math_evaluator.py` | **直接删除** | 不再保留任何代码 |
| `src/utils/dpo_quality_evaluator.py` | **直接删除** | 不再保留任何代码 |

> 原补充方案 A（vLLM-only）中针对这三个 evaluator 的改造内容（删除本地模型分支、保留 API 路径等）**全部作废**；因为文件整体删除，无需再做内部改造。

---

### B.2 需要同步修改的文件清单

三个 evaluator 被删除后，以下文件中的引用、映射、聚合逻辑必须同步清理：

1. **`src/utils/task_types.py`** —— `EvaluationTaskType` 枚举需删除 `MATH`、`CODE`、`DPO_QUALITY`。
2. **`src/api/routers/evaluation.py`** —— 需删除：
   - `_get_evaluator_class()` 中的 `MATH` / `CODE` / `DPO_QUALITY` 分支
   - `execute_evaluation_task()` 中的 `MATH` / `CODE` / `DPO_QUALITY` 聚合分支
   - `_aggregate_math()`、`_aggregate_code()`、`_aggregate_dpo()` 三个聚合函数
   - `EvaluatorConfig` 中仅属于被删 evaluator 的配置字段
   - `EvaluationRequest` 中与被删 evaluator 强相关的字段（如有）
3. **`src/api/main.py`** / **`src/api/eval_minimal_main.py`** —— 无需修改（不直接引用具体 evaluator）。
4. **`src/utils/__init__.py`** —— 若此前曾导出这三个类，需删除导出（当前未导出，保持现状即可）。

---

### B.3 Router 层具体改造细节

#### B.3.1 `EvaluationTaskType` 枚举精简

```python
class EvaluationTaskType(str, Enum):
    MEDQA_LLM = "medqa_llm"
    BLEU_ROUGE = "bleu_rouge"
    # 以下三项删除
    # MATH = "math"
    # CODE = "code"
    # DPO_QUALITY = "dpo_quality"
```

#### B.3.2 `_get_evaluator_class()` 精简

```python
def _get_evaluator_class(task_type: EvaluationTaskType):
    if task_type == EvaluationTaskType.MEDQA_LLM:
        from utils.medqa_llm_evaluator import MedQALLMEvaluator
        return MedQALLMEvaluator
    elif task_type == EvaluationTaskType.BLEU_ROUGE:
        from utils.bleu_rouge_evaluator import BleuRougeEvaluator
        return BleuRougeEvaluator
    # 删除 MATH / CODE / DPO_QUALITY 分支
    else:
        raise ValueError(f"不支持的评估任务类型: {task_type}")
```

#### B.3.3 聚合函数删除

删除以下三个函数：
- `_aggregate_math()`
- `_aggregate_code()`
- `_aggregate_dpo()`

保留：
- `_aggregate_medqa()`
- `_aggregate_bleu()`

#### B.3.4 `execute_evaluation_task()` 精简

删除 `try` 块中的三个 `elif` 分支：

```python
# 删除以下分支
elif task_type == EvaluationTaskType.MATH:
    metrics = _aggregate_math(rows_out)
elif task_type == EvaluationTaskType.CODE:
    metrics = _aggregate_code(rows_out)
elif task_type == EvaluationTaskType.DPO_QUALITY:
    metrics = _aggregate_dpo(rows_out)
```

#### B.3.5 `EvaluatorConfig` 精简

删除仅属于被删 evaluator 的字段：

```python
class EvaluatorConfig(BaseModel):
    model_name_or_path: Optional[str] = Field(default=None)
    device: str = Field(default="cpu")
    max_new_tokens: int = Field(default=2048)
    temperature: float = Field(default=0.7)
    top_p: float = Field(default=0.9)
    vllm_model_name: Optional[str] = Field(default=None)
    vllm_base_url: Optional[str] = Field(default=None)
    vllm_api_key: Optional[str] = Field(default=None)
    prompt_key: str = Field(default="prompt")
    ground_true_answer_key: str = Field(default="answer")
    padding_side: str = Field(default="left")
    use_fast: bool = Field(default=True)
    # 以下字段删除（math 专用）
    # enable_thinking: bool = Field(default=False)
    # max_length: int = Field(default=2048)
    # 以下字段删除（dpo 专用）
    # beta: float = Field(default=0.1)
    # query_key: str = Field(default="query")
    # chosen_key: str = Field(default="chosen")
    # rejected_key: str = Field(default="rejected")
    # 以下字段删除（code 专用）
    # execution_timeout: int = Field(default=30)
    # max_memory_mb: int = Field(default=256)
    # sandbox_working_dir: str = Field(default="/tmp/code_eval")
    # enable_security_check: bool = Field(default=True)
```

> **说明**：`padding_side`、`use_fast` 在 `BleuRougeEvaluator` 中仍通过 `.get()` 读取（用于 tokenizer 初始化），但鉴于 vLLM-only 改造后 `BleuRougeEvaluator` 也删除本地模型分支，这两个字段实际上不再被使用。若希望进一步精简，可一并删除。为保持最小改动，此处建议先保留，待 vLLM-only 改造时统一清理。

---

### B.4 目录结构终态（再次更新）

```
src/utils/
├── __init__.py
├── async_eval_queue.py         ← 已改造（注入式执行函数）
├── base_evaluator.py           ← 已改造（删除 settings、删除 load_model_and_tokenizer 待 vLLM-only 时执行）
├── bleu_rouge_evaluator.py     ← 已改造（删除 evaluate() / run() / settings）
├── file_utils.py
├── gspo_utils.py
├── inference_utils.py          ← 从 evaluation 移入
├── json_parse_utils.py
├── logger.py
├── medqa_llm_evaluator.py      ← 已改造（删除 evaluate() / run() / settings）
├── metrics.py
├── task_types.py               ← 需删除 MATH/CODE/DPO_QUALITY
└── text_utils.py

src/api/routers/evaluation.py   ← 需删除 MATH/CODE/DPO 相关映射、聚合、配置
```

> `src/utils/` 下不再存在 `code_evaluator.py`、`math_evaluator.py`、`dpo_quality_evaluator.py`。

---

### B.5 实施步骤

按以下顺序执行：

1. **删除三个 evaluator 文件**
   ```bash
   rm src/utils/code_evaluator.py
   rm src/utils/math_evaluator.py
   rm src/utils/dpo_quality_evaluator.py
   ```

2. **精简 `task_types.py`**
   - 删除 `MATH`、`CODE`、`DPO_QUALITY` 枚举项。
   - 验证：`python -m py_compile src/utils/task_types.py`

3. **改造 `evaluation.py` Router**
   - 删除 `_aggregate_math`、`_aggregate_code`、`_aggregate_dpo`。
   - 删除 `_get_evaluator_class` 中三个分支。
   - 删除 `execute_evaluation_task` 中三个 `elif` 分支。
   - 精简 `EvaluatorConfig`（删除 math/dpo/code 专用字段）。
   - 验证：`python -m py_compile src/api/routers/evaluation.py`

4. **端到端验证**
   - 启动 API：`cd src/api && python main.py`
   - 测试 `MEDQA_LLM` 与 `BLEU_ROUGE` 两种任务类型正常执行。
   - 测试请求 `task_type: "math"` 时应返回 `ValueError`（不支持的评估任务类型）。

---

### B.6 风险提示

| 风险点 | 影响 | 缓解措施 |
|---|---|---|
| **旧请求若传入 `math` / `code` / `dpo_quality` task_type 会失败** | 中 | Router 返回明确的 `ValueError: 不支持的评估任务类型`，引导调用方改用保留的两种类型 |
| **DPO / Math / Code 评估能力永久下线** | 高（若业务后续需要） | 如需恢复，可从 git 历史记录中找回原文件；建议执行前 `git commit` 备份当前状态 |
| **Checklist 与原文档多处引用需同步更新** | 低 | 本补充方案已覆盖所有引用点；实施时以本章节为准，原文档中与之冲突的描述视为作废 |

---

### B.7 快速检查清单（Checklist）

- [ ] `src/utils/code_evaluator.py` 已删除
- [ ] `src/utils/math_evaluator.py` 已删除
- [ ] `src/utils/dpo_quality_evaluator.py` 已删除
- [ ] `src/utils/task_types.py` 中仅剩 `MEDQA_LLM` 与 `BLEU_ROUGE`
- [ ] `src/api/routers/evaluation.py` 中无 `_aggregate_math` / `_aggregate_code` / `_aggregate_dpo`
- [ ] `src/api/routers/evaluation.py` 中 `_get_evaluator_class` 仅含 `MEDQA_LLM` 与 `BLEU_ROUGE`
- [ ] `src/api/routers/evaluation.py` 中 `EvaluatorConfig` 无 math/dpo/code 专用字段
- [ ] `python -m py_compile src/api/routers/evaluation.py` 通过
- [ ] `python -m py_compile src/utils/task_types.py` 通过
- [ ] API 启动无 ImportError
